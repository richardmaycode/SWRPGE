= form_for([:encyclopedia, @species]) do |f|
  = f.label :name
  = f.text_field :name
  %br
  %br
  = f.label :description
  = f.rich_text_area :description
  = f.label :source_id, 'Source'
  %br
  = f.select :source_id, options_from_collection_for_select(Source.all.collect, 'id', 'name')
  %br
  %br
  = f.submit
  = link_to('cancel', @species.new_record? ? [:index, :encyclopedia, :species] : [ :encyclopedia, @species])